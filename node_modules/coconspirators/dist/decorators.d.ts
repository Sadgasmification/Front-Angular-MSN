import { QueueOptions, ExchangeOptions } from './types';
/**
 * Denotes a queue and registers QueueOptions against it
 *
 * @export
 * @param {QueueOptions} props
 * @returns
 */
export declare function Queue(props: QueueOptions): (target: any) => void;
/**
 * Denotes an exchange and registers ExchangeOptions against it
 *
 * @export
 * @param {EchangeOptions} props
 * @returns
 */
export declare function Exchange(props: ExchangeOptions): (target: any) => void;
